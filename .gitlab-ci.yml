stages:
  - test

test_vars:
  stage: test
  tags:
    - docker
  script:
    - echo "Username: $DOCKERHUB_USERNAME"
    - docker login -u "$DOCKERHUB_USERNAME" -p "$DOCKERHUB_PASSWORD"
    - docker --version
